# 📊 Task Planner 3D

Сервис для управления задачами с 3D визуализацией в виде интерактивного графа.

## 🌟 Функционал

### Визуализация 3D
- **Цветовая индикация статуса задач:**
  - 🔴 Красный - заблокированная задача
  - 🔵 Синий - задача в работе
  - 🟢 Зеленый - завершенная задача
  - ⚪ Серый - задача к выполнению

### Связи между задачами
- **Стрелочные ребра** - связь от родительской задачи к дочерней
- **Обычные ребра** (пунктирные) - простая связь между задачами

### Символы задач
Каждая задача имеет свой символ-иконку для быстрой идентификации типа:
- 📝 Обычная задача
- 💼 Рабочая
- 🎯 Цель
- 🚀 Запуск
- ⚡ Срочно
- 🔥 Важно
- 💡 Идея
- 🎨 Дизайн
- 🔧 Техническая
- 📊 Аналитика
- 🏆 Достижение
- ⭐ Особенная

### Фильтры и группировки
- Фильтрация по статусу (к выполнению, в работе, заблокирована, завершена)
- Фильтрация по тегам (general, work, personal, urgent, research, design, development, bug, feature)
- Отображение статистики по задачам

### Управление задачами
- ➕ Добавление новых задач с кастомными параметрами
- ✏️ Редактирование существующих задач
- 🗑️ Удаление задач
- 📍 Управление позицией в 3D пространстве

## 🚀 Установка и запуск

### Предварительные требования
- Node.js (версия 14 или выше)
- npm или yarn

### Установка Node.js

Если у вас не установлен Node.js, установите его одним из способов:

#### macOS (рекомендуется Homebrew):
```bash
# Установка Homebrew (если еще не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установка Node.js
brew install node
```

#### Или скачайте установщик:
Скачайте и установите с официального сайта: https://nodejs.org/

#### Проверка установки:
```bash
node --version
npm --version
```

Обе команды должны вывести номер версии.

### Шаг 1: Установка зависимостей

```bash
cd ~/Desktop/planner
npm run install-all
```

Эта команда установит все зависимости для сервера и клиента.

### Шаг 2: Запуск приложения

#### Вариант 1: Запуск всего приложения (рекомендуется)
```bash
npm run dev
```

Эта команда запустит одновременно:
- Backend сервер на `http://localhost:5001`
- Frontend приложение на `http://localhost:3000`

#### Вариант 2: Раздельный запуск

**Терминал 1 - Backend:**
```bash
npm run server
```

**Терминал 2 - Frontend:**
```bash
npm run client
```

### Шаг 3: Открыть приложение

Откройте браузер и перейдите по адресу: `http://localhost:3000`

## 📖 Использование

### Добавление задачи
1. Нажмите кнопку "➕ Добавить задачу" в левой панели
2. Заполните форму:
   - **Название** - краткое название задачи
   - **Описание** - детальное описание (необязательно)
   - **Статус** - текущий статус задачи
   - **Тег** - категория задачи
   - **Символ** - иконка для задачи
   - **Позиция** - координаты X, Y, Z в 3D пространстве
3. Нажмите "Создать"

### Редактирование задачи
1. Кликните на ноду (сферу) в 3D графе
2. Измените нужные поля
3. Нажмите "Сохранить"

### Удаление задачи
1. Кликните на задачу для редактирования
2. Нажмите кнопку "🗑️ Удалить"
3. Подтвердите удаление

### Навигация в 3D
- **Вращение** - левая кнопка мыши + перетаскивание
- **Перемещение** - правая кнопка мыши + перетаскивание
- **Масштабирование** - колесико мыши

### Импорт из Excel
1. Подготовьте файл `.xlsx` с колонками **БЕЗ заголовков** в таком порядке:
   - **Колонка A**: ID задачи (число, например: 1, 2, 5, 10)
   - **Колонка B**: Символ (emoji или текст, например: 🧘, A, 🔥)
   - **Колонка C**: Название задачи
   - **Колонка D**: ID родительской задачи (пусто если корневая)
   - **Колонка E**: ID связанных задач через запятую (например: 2,3,5)
   - **Колонка F**: Описание (необязательно)
   - **Колонка G**: Статус (В работе, Решен, Заблокирован, и т.д.)

2. Нажмите **"📂 Импорт из Excel"** в левой панели
3. Выберите ваш `.xlsx` файл
4. Нажмите **"🚀 Импортировать"**
5. Подтвердите замену данных
6. Задачи автоматически загрузятся с умным позиционированием!

**Умное позиционирование:**
- ✅ Задачи с одинаковым символом группируются рядом ("букетом")
- ✅ Корневые задачи ближе к центру (r=5)
- ✅ Каждый уровень вложенности отдаляется: +4 к радиусу
- ✅ Автоматическое создание связей parent→child (желтые стрелки)
- ✅ Обычные связи создаются из колонки E (серый пунктир)

**Поддерживаемые статусы:**
- "В работе", "In Progress" → синий (in_progress)
- "Решен", "Завершен", "На приёмке", "Completed" → зеленый (completed)
- "Заблокирован", "Blocked" → красный (blocked)
- Любой другой или пустой → серый (todo)

### Фильтрация
Используйте выпадающие списки в левой панели:
- **По статусу** - показать только задачи с определенным статусом
- **По тегу** - показать только задачи с определенным тегом

## 🗄️ Структура проекта

```
planner/
├── server/
│   ├── index.js          # Backend сервер (Express + SQLite)
│   └── planner.db        # База данных (создается автоматически)
├── client/
│   ├── public/
│   │   └── index.html    # HTML шаблон
│   └── src/
│       ├── components/   # React компоненты
│       │   ├── TaskGraph3D.js      # 3D визуализация
│       │   ├── ControlPanel.js     # Панель управления
│       │   └── TaskModal.js        # Модальное окно задачи
│       ├── App.js        # Главный компонент
│       └── index.js      # Точка входа
├── package.json          # Зависимости сервера
└── README.md            # Документация
```

## 🔧 API Endpoints

### Tasks
- `GET /api/tasks` - получить все задачи
- `GET /api/tasks/:id` - получить задачу по ID
- `POST /api/tasks` - создать новую задачу
- `PUT /api/tasks/:id` - обновить задачу
- `DELETE /api/tasks/:id` - удалить задачу

### Relations
- `GET /api/relations` - получить все связи
- `POST /api/relations` - создать новую связь
- `DELETE /api/relations/:id` - удалить связь

## 🎨 Технологии

### Backend
- **Node.js** - серверная среда выполнения
- **Express** - веб-фреймворк
- **better-sqlite3** - встраиваемая база данных

### Frontend
- **React** - библиотека для UI
- **Three.js** - 3D графика
- **@react-three/fiber** - React renderer для Three.js
- **@react-three/drei** - готовые 3D компоненты
- **Axios** - HTTP клиент

## 💡 Возможности для расширения

1. **Связи между задачами**: добавить интерфейс для создания связей через UI
2. **Перетаскивание нод**: возможность перемещать ноды мышью в 3D
3. **Поиск**: добавить поиск задач по названию/описанию
4. **Экспорт/Импорт**: сохранение и загрузка проектов
5. **Подзадачи**: иерархическая структура задач
6. **Сроки**: добавить дедлайны и напоминания
7. **Комментарии**: обсуждение задач
8. **История изменений**: отслеживание изменений задач
9. **Темы оформления**: светлая/темная тема
10. **Мультиязычность**: поддержка разных языков

## 📝 Лицензия

MIT License - свободно используйте и модифицируйте проект.
